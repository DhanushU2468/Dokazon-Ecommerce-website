#About my application 
 -> This is E-commerce web Application
 ->Toast used in application for notification and popups
 #JSON server to run the command
 ->npx json-server --watch src/Database/product.json --port 4000
